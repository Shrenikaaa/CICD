name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Backend Service
        run: curl -v ${{ secrets.BACKEND_HOOK }}

      - name: Deploy Frontend Service
        run: |
          curl -v -X POST \
            -H "Content-Type: application/json" \
            -d '{ "buildArgs": { "API_KEY": "${{ secrets.RENDER_BACKEND_URL }}" } }' \
            ${{ secrets.FRONTEND_HOOK }}